const mailOptions = {
  from: process.env.EMAIL_USER,              // hamesha fixed rahega
  replyTo: req.body.email,                   // form wale ka email — reply button se seedha unhe jayega
  to: process.env.CONTACT_RECEIVER_EMAIL,
  subject: `New message from ${req.body.name}`,
  text: `
Name: ${req.body.name}
Email: ${req.body.email}

Message:
${req.body.message}
  `,
};